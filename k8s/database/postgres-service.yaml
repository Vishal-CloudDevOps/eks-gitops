apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: app
spec:
  clusterIP: None   # headless service, standard pattern for a StatefulSet
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
